name: custom_navigator
description: Just a fork.
version: 0.2.5
author: bradintheusa@gmail.com
homepage: https://github.com/bradintheusa/Custom-navigator

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

# The following section is specific to Flutter.
flutter:

